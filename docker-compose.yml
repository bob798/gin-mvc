services:
  app:
    build: .
    image: md-preview:latest
    container_name: md-preview
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/backend/data
    environment:
      PORT: "8000"
